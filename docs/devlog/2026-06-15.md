# 2026-06-15

## Sesión 16:05

### Contexto
Enviar la campaña de captación del **Diplomado 4ª generación** por WhatsApp a la base externa (194 contactos de una masterclass previa). Como el WhatsApp propio aún no está verificado, se usó el WABA verificado de **Capital Inteligente** (credenciales reutilizadas de `encuentrosmart`). Operación de punta a punta: limpiar base → submitear plantilla → aprobar → probar → blast.

### Lo que se hizo
- **Limpieza de la base:** `BD Externa.xlsx` (194 filas) → **169 teléfonos E.164** únicos y válidos (2 duplicados, 23 sin número, 0 dudosos). Resultado en `docs/marketing/telefonos-bd-externa.csv`.
- **Credenciales:** copiadas las `WHATSAPP_CLOUD_*` (version, phone_number_id, WABA_ID, access_token, app_secret, app_id, enabled) desde `encuentrosmart` al `.env` (gitignored). Token validado contra el WABA antes de operar.
- **Dos scripts nuevos:**
  - `scripts/whatsapp-submit-template.mjs` — sube el brochure como header y crea la plantilla en Meta (`--submit`, `--list`).
  - `scripts/send-diplomado-whatsapp.mjs` — envío idempotente con throttle, soporta `--send`, `--only`, dry-run.
- **Plantilla `diplomado_4ta_gen_captacion`** (MARKETING, es): header = brochure PDF (1.4 MB), body con `{{1}}` = primer nombre capitalizado, 2 botones. Datos canónicos: 20 de junio · 12 semanas · híbrida.
- **Flujo de aprobación:** submit → `PENDING` (id `1287597989793614`) → **`APPROVED` a las 15:45** (vigilada con un poll en background cada 3 min, exit 0 = aprobada).
- **Prueba real** al número del usuario (`+569…`) → OK (brochure adjunto, nombre personalizado, botones funcionando). Ajuste menor: `--only` ahora permite un número de prueba aunque no esté en el CSV.
- **Blast completo: 169/169 · 0 fallos.** Cada mensaje con su `wamid`. Log en `docs/marketing/whatsapp-envio-resultados.json`.
- Memoria `reference-whatsapp-campaign-setup.md` y `docs/codemap.md` actualizados. Se pasó por `spec-flow` (telemetría: +2 eventos).

### Lo que se descartó
- **Chatbot con lógica de cierre** (`Guion Chatbot con Lógica de Cierre.docx`): se evaluó montarlo para atender la ventana de 24h, pero el usuario decidió NO construirlo para resolver el envío de hoy. La plantilla solo enruta a **postular** (formulario) o **hablar con un humano**; el brochure va adjunto. Consecuencia: sin webhook ni tracking de entrega/lectura por código.
- **Botón `wa.me` ("Hablar con un asesor" → chat de Paola):** Meta rechazó la plantilla en el primer submit — **no acepta enlaces `wa.me` en botones URL** (`Invalid parameter / OAuthException`). El brochure sí se subió bien (handle obtenido). Se reemplazó por un **botón de LLAMADA** (`PHONE_NUMBER`) a +56 9 8673 3726 y el resubmit pasó a `PENDING`.

### Pendientes / dudas abiertas
- **Sin commitear:** los scripts nuevos, el CSV y el cambio de codemap quedan en el working tree. El usuario no respondió si armar el commit `feat` → queda pendiente.
- **Atención manual del número de campaña** (+56966227368): sin bot, quien responda ahí en lugar de tocar un botón queda sin atención automática. Alguien debe revisar ese WhatsApp hoy/mañana.
- **Monitoreo en WhatsApp Manager** (cuenta Capital Inteligente): entrega/lectura real (no trackeada por código) y **quality rating** del número en las próximas horas, por riesgo de bloqueos/reportes.
- **WhatsApp propio aún no verificado:** se usó el de Capital Inteligente como puente; verificar el propio para futuras campañas.
