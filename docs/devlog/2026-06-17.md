# 2026-06-17

## Sesión 12:24

**Contexto**: El usuario no encontraba cómo editar el calendario del diplomado desde el admin. Resultó ser un problema de descubribilidad, no de funcionalidad faltante.

**Lo que se hizo**:
- Verifiqué que el editor de calendario YA existe en `app/(admin)/admin/cohorts/[cohortId]/sesiones/` (`page.tsx` + `sessions-manager-client.tsx`, commit `002ca7a`). Estaba enterrado: tab "Información" → tarjeta "Acciones rápidas" → botón "Gestionar calendario", dos niveles abajo y junto a acciones deshabilitadas.
- Decisión de UX (elegida por el usuario, opción 1): agregar un tab dedicado **"Calendario"** en la barra de pestañas de la cohorte.
- Edité `cohort-detail-client.tsx`: el nuevo tab es un `Link` a `/admin/cohorts/[id]/sesiones` (los otros tres tabs cambian estado interno; este navega porque `sesiones` es una página server-side independiente que carga sesiones/instructores/recursos desde Supabase).
- Extraje la clase del tab a una constante `TAB_BTN_CLASS` para que botón y link se vean idénticos sin duplicar el string.
- `npx tsc --noEmit` pasó limpio. `Link` y `CalendarIcon` ya estaban importados.

**Lo que se descartó**:
- Opción 2 (botón primario "Gestionar calendario" arriba, junto al título): descartada en favor del tab dedicado por consistencia con el patrón de tabs existente.
- No se quitó el botón viejo de "Acciones rápidas" → se dejó como redundancia inofensiva (pendiente de decisión del usuario).

**Pendientes / dudas abiertas**:
- Falta probar en vivo: no se abrió el navegador para confirmar que el tab navega y el editor carga las sesiones del diplomado.
- No se pusheó (según el flujo, push solo bajo pedido explícito). Cambio queda en local.
- Decidir si se elimina el botón redundante en "Acciones rápidas".

## Sesión 12:24 — cuotas con recargo en el cobro genérico

**Contexto**: Añadir al cobro genérico la opción de pagar en cuotas (con su recargo), replicando el comportamiento que ya existe en los checkouts del Diplomado y Liderazgo.

**Lo que se hizo**:
- Pasé la petición por `spec-flow` → clasificada `feat`, small-medium, riesgo medio (toca dinero + frontera de firma HMAC), Tier 2.
- Verifiqué en código los factores de recargo (idénticos en ambos checkouts): **6 cuotas ×1,0668 (+6,68%)**, **12 cuotas ×1,1018 (+10,18%)**, en `lib/flow/checkout.ts` y `lib/programs/liderazgo.ts`.
- `lib/cobro/plans.ts` (nuevo): planes `contado` / `cobro-6` / `cobro-12` con recargo **basado en factor** (no monto absoluto, porque el monto base del cobro es variable) + `resolveCobroAmount(base, plan)`.
- `app/api/pago/cobro/route.ts`: acepta `plan` (default `contado`); recomputa el recargo server-side; pasa `paymentMethodOverride` a Flow (`1` Webpay para cuotas, `9` todos los medios para contado) y el `subjectSuffix`; guarda `amount_clp` = monto final.
- `app/pago/cobro/CobroCheckoutClient.tsx`: selector de 3 opciones de pago con monto calculado, nota de recargo, total y botón reflejando el monto final.
- Actualicé `docs/codemap.md`. Typecheck limpio; lint sin errores (solo un warning preexistente de `watch()` en RHF que no toqué).
- Commit acotado solo a los archivos de la feature (el working tree tenía cambios de classroom no relacionados que dejé intactos): `84b124b` feat + `20995c8` docs(changelog). Push a `origin` (`elkisdm/Capitalacademy`) y a `work` (`edaza-create/capitalacademy`).

**Decisiones tomadas**:
- La **firma HMAC sigue cubriendo solo el monto base** (contado). El servidor recomputa el monto final con el factor del plan elegido → el pagador solo puede pagar igual o más (recargo), nunca menos; el factor es server-side y determinista.
- El plan lo elige el **pagador en el checkout**, no el admin al generar el link → el generador de `/admin/cobros` NO cambia.
- `payments.plan` se mantiene en `null` (su CHECK solo acepta planes del Diplomado); el plan de cuotas no se persiste en esa columna.
- Solo aplica a **Flow** (el cobro genérico ya era Flow-only; Fintoc no soporta cuotas con recargo).
- Compatibilidad: links de cobro existentes siguen válidos → default `contado`, sin recargo.

**Lo que se descartó / no funcionó**:
- Primer `git push work main` se quedó **colgado en background**. Diagnóstico inicial errado: que el remoto HTTPS esperaba credenciales para `edaza-create`. Causa real (tras aclaración del usuario: el gh CLI ya tiene ambas cuentas): git **no estaba usando gh como credential helper**.
- Fix: maté el push colgado, corrí `gh auth setup-git` (cuenta activa `edaza-create`, dueña del remoto `work`) y reintenté con `GIT_TERMINAL_PROMPT=0`. Push exitoso (`8a60329..20995c8`). Commit ahora en ambos remotos.

**Pendientes / dudas abiertas**:
- No se probó localmente con `pnpm dev` (el usuario pidió commit/push directo). Falta validar manualmente el selector de cuotas contra Flow real.

## Sesión 12:24 — 404 en classroom + login/onboarding branded por entorno

**Contexto**: Dos frentes — (1) un usuario reportó **404 en `/classroom/diplomado-iv-generacion`** incluso siendo "Administrador"; (2) feature nueva: **login y onboarding branded por entorno/programa** (Diplomado, Liderazgo, Workshop Inmobiliario) en vez del `/login` y `/onboarding/complete-profile` único y global. La feature pasó por `spec-flow` (Tier 3 Full).

**Lo que se hizo — Bug 404 del classroom**:
- Diagnóstico: el cohort y el slug eran correctos y los 9 alumnos matriculados entraban bien. El 404 lo veía solo `admin@capitalacademy.cl` (rol `admin`) porque NO estaba matriculado. Las 4 páginas del classroom hacían `if (!enrollment) notFound()`, exigiendo matrícula activa incluso a staff — contradiciendo **ADR-0004** (admin = acceso completo).
- Fix de datos (inmediato): INSERT en `public.enrollments` matriculando la cuenta admin como `active` en el cohort G4, con `on conflict do nothing`. Aplicado directo vía MCP Supabase (sin migración versionada).
- Fix de código (raíz):
  - Nuevo `lib/classroom/access.ts` con `getClassroomAccess()`: concede acceso si hay matrícula activa **o** rol staff (admin/ops); `null` solo si ninguna.
  - `lib/classroom/queries.ts`: `getModulesWithLessons` y `getLessonProgress` toleran `enrollmentId` nulo (staff ve módulos sin progreso personal).
  - Las 4 páginas (`classroom/[cohortSlug]/page.tsx`, `[moduleSlug]/page.tsx`, `[moduleSlug]/[lessonSlug]/page.tsx`, `calendario/page.tsx`) usan el helper en vez del gate de matrícula.
  - Verificado que las RLS de `program_modules` y `lessons` permiten lectura a cualquier autenticado (`auth.uid() is not null`). `tsc --noEmit` limpio.

**Lo que se hizo — Feature login/onboarding por entorno (Tier 3)**:
- Clasificación spec-flow: `feat` · large · high risk (auth) · Tier 3 Full.
- Decisiones del usuario (AskUserQuestion): el entorno es el tenant completo; solo los admins son transversales. URLs: **ambas** (genérica + namespaced). Login real único (cuenta = email+contraseña); lo branded es solo la **página**, no la autenticación.
- Estado actual: 2 entornos con classroom — `DIP-VENTAS` (cohort `diplomado-iv-generacion`) y `WS-INMOB` (cohort `workshop-inmobiliario-mayo-2026`). Liderazgo es solo checkout (sin cohort).
- Archivos:
  - `lib/programs/registry.ts` (nuevo) — fuente única de identidad por entorno, keyed por slug estable (`diplomado`, `workshop`, `liderazgo`); acentos: Diplomado=violet, Workshop=navy, Liderazgo=amber; helper de paths branded.
  - `app/(auth)/login/login-screen.tsx` (nuevo, shell compartido); `login-form.tsx` acepta `accent`; `login/page.tsx` reescrito con marca por defecto; `login/[programa]/page.tsx` (nuevo).
  - `app/onboarding/set-password/set-password-form.tsx` acepta `brand`+`loginPath`; `app/onboarding/[programa]/set-password/page.tsx` (nuevo).
  - `app/(onboarding)/onboarding/complete-profile/{complete-profile-client,page}.tsx` aceptan `brand` opcional derivado de la matrícula; `onboarding/[programa]/complete-profile/page.tsx` (nuevo).
  - `lib/classroom/enroll-from-payment.ts` — el comprador del Diplomado recibe link branded `/onboarding/diplomado/set-password`.
  - `lib/supabase/middleware.ts` — whitelist ampliado para la variante branded del set-password (pública; el invitado aún no tiene sesión).
  - `docs/codemap.md` — nueva sección "Acceso / Onboarding". `.spec-flow/events.jsonl` — telemetría.
- Verificación: `tsc --noEmit` limpio + `next build` verde; las rutas nuevas resuelven sin colisión.

**Lo que se descartó**:
- **Hipótesis enlace/slug roto**: descartada — el cohort existe, el slug es correcto y los 9 matriculados entran. El problema no era el link.
- **Sospecha de que era la cuenta de Elkis (admin)**: descartada — Elkis SÍ está matriculado. El "Administrador" del screenshot era `admin@capitalacademy.cl`, sin matrícula.
- **Branding por logo por programa**: descartado, solo existe el logo de Capital Academy. La identidad por entorno se diferencia vía **color de acento + nombre + copy**, reusando el logo CA.
- **Repintar cada token de color en `CompleteProfileClient`**: descartado para no inflar el diff (está muy tokenizado con violet). Se brandeó solo lo de alta señal (nombre del entorno, título/subtítulo, redirect final) vía prop `brand` opcional.
- **Onboarding fields divergentes por entorno**: no se construyó (evitar sobre-ingeniería). Set de campos compartido; registry preparado para divergir vía hook `onboardingFields` opcional a futuro.
- **Riesgo corregido a tiempo**: el middleware permitía `/onboarding/set-password` pero NO la variante branded `/onboarding/diplomado/set-password` — habría bloqueado a los invitados branded. Se ajustó el whitelist.

**Pendientes / dudas abiertas**:
- No se hizo `push` — el código quedó local esperando confirmación (política: testear local, push solo bajo pedido explícito).
- Liderazgo está en el registry pero sin cohort/classroom (solo checkout); su login/onboarding branded existe pero sin flujo real detrás.
- Los scripts de invitación (más allá de `enroll-from-payment.ts`) aún emiten el link genérico `/onboarding/set-password`; solo se actualizó el flujo del comprador del Diplomado. El genérico se re-brandea desde la matrícula, pero los scripts no emiten el link branded.

**Referencias**:
- **ADR-0004** (RBAC): admin con acceso completo — el fix del 404 lo restituye.
- Fix de datos en producción: INSERT en `public.enrollments` (admin → cohort G4), vía MCP Supabase. Sin archivo de migración versionado.
- Telemetría spec-flow: entrada `login-onboarding-por-entorno` (tier 3, feat, large, high risk) en `.spec-flow/events.jsonl`.
- Sin PRs ni issues; sin push.
