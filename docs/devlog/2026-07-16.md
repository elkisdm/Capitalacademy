# 2026-07-16

## Sesión 14:57

Corrida automatizada de una tarea agendada (`verificar-ensayo-cron-recordatorios`): verificación de **solo lectura** contra producción del ensayo del nuevo fan-out de recordatorios de clase. Sin cambios de código.

### Contexto

A las 12:48 (Chile) se había desplegado a prod (`fba1563`) el rediseño del envío de recordatorios: lotes de Resend + bitácora de idempotencia **por destinatario** (`session_reminder_recipients`, migración 0075). El ensayo era el recordatorio '24h' de la sesión de Liderazgo (`e0000000-…-0302`, viernes 17-jul 15:00) con 3 destinatarios, antes del evento real del lunes 20-jul (239 correos del Ciclo CI).

### Lo que se hizo

- Verificadas ambas consultas contra prod (proyecto `igatsyghbadccbrjiurl`), corridas a las 14:41 Chile:
  - `session_reminder_recipients` (kind='24h'): **3 filas**, todas `sent`, `error` null, escritas a las 14:37:40.
  - `session_reminders` (kind='24h'): **1 fila** `status='sent'`, `recipients_count=3`, `error` null.
  - Orden de timestamps confirma el diseño nuevo: Resend responde (14:37:38.7) → se marca la sesión (14:37:39.5) → se escribe la bitácora por destinatario (14:37:40.0).
- **Veredicto: GO** para el evento del lunes. La condición de falla que se buscaba (session_reminders en `sent` con bitácora vacía) NO ocurrió: la idempotencia por destinatario sí se está escribiendo.
- Actualizada la memoria del proyecto (`project-megaauditoria-global-2026-07-16.md`) para marcar C4 como cerrado y ensayado en prod.

### Lo que se descartó

- Ninguna estrategia técnica se probó/descartó (sesión de verificación, no de desarrollo).
- Salvedad sobre el GO: el ensayo prueba la **corrección del mecanismo** (3 destinatarios), no el comportamiento bajo el rate limit de Resend a escala 80× mayor (239 el lunes). Ese riesgo es justamente lo que los lotes + bitácora por destinatario mitigan: si un lote falla, esas filas quedan sin `sent` y son reintentables en vez de morir en estado terminal.

### Pendientes / dudas abiertas

- Evento real: lunes 20-jul 09:30 Chile, 239 recordatorios del Ciclo CI. Vigilar bajo carga real.
- Siguiente pendiente con fecha de la megaauditoría: alerta de inasistencias (martes 28-jul).
- Fricción operativa: el gate de orquestación bloquea `mcp__supabase__execute_sql` incluso para SELECT (lo clasifica como operación MCP mutante), lo que obligó a delegar las lecturas al subagente `ejecutor`. Para que las verificaciones agendadas de solo lectura corran directo habría que afinar el gate para distinguir SELECT del resto.

### Referencias

- Informe: `docs/audit/2026-07-16-megaauditoria-global.md` (hallazgo C4).
- Deploy del rediseño: commit `fba1563`.
