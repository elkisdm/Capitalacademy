# 2026-07-18

## Sesión 10:19

**Contexto**: corrida automática (scheduled task `segunda-tanda-ciclo-ci`) de la segunda tanda de invitaciones del Ciclo de Capacitación Comercial CI, autorizada por Elkis el 16-jul para hoy 10:00. Los enlaces de la primera tanda (15-jul) expiraban hoy y la clase es el martes 21-jul 10:00.

**Lo que se hizo**:
- Dry-run de `scripts/reinvite-stuck-capacitaciones.mjs`: 239 matriculados `active` en el cohorte, 64 ya activados, **175 pendientes** (117 venían de variante `invite` en tanda 1, 58 de `recovery`).
- Sanity check OK (175 > 0 y ≤ 239, dry-run limpio) → se ejecutó el envío real (`--send`, delegado a subagente ejecutor).
- **Resultado: 175/175 correos enviados, 0 fallidos**, ~3,5 min. Cada envío quedó registrado en `invitation_log`. En esta tanda todos los links salieron como `type=recovery` porque las cuentas ya existen desde la tanda 1 (comportamiento esperado).
- No se tocó git, no se modificó el script, no se envió ninguna otra campaña.

**Pendientes / dudas abiertas**:
- Ver cuántos de los 175 activan su cuenta antes de la clase del martes 21-jul 10:00 (quien no active no puede marcar asistencia).
- Los hooks de sesión siguen reportando 6 commits sin entrada en `CHANGELOG.md` y 6 cambios estructurales sin reflejar en `docs/codemap.md`; quedaron intactos por estar fuera del alcance de la tarea.
- La duración del envío es estimada (tiempo total de la invocación); el script no reporta timestamps propios.
